<?php

declare(strict_types=1);

namespace BitBag\PPClient\Model;

final class PocztexPackageSizeEnum
{
    public const S = 'S';

    public const M = 'M';

    public const L = 'L';

    public const XL = 'XL';

    public const DOUBLE_XL = '2XL';

    public const MAX_HEIGHT_S = 9;

    public const MAX_WIDTH_S = 40;

    public const MAX_DEPTH_S = 65;

    public const MAX_WEIGHT_S = 20;

    public const MAX_HEIGHT_M = 20;

    public const MAX_WIDTH_M = 40;

    public const MAX_DEPTH_M = 65;

    public const MAX_WEIGHT_M = 20;

    public const MAX_HEIGHT_L = 42;

    public const MAX_WIDTH_L = 40;

    public const MAX_DEPTH_L = 65;

    public const MAX_WEIGHT_L = 20;

    public const MAX_HEIGHT_XL = 60;

    public const MAX_WIDTH_XL = 60;

    public const MAX_DEPTH_XL = 70;

    public const MAX_WEIGHT_XL = 20;

    public const MAX_DIMENSIONS_2XL = 250;

    public const MAX_DEPTH_2XL = 120;

    public const MAX_WEIGHT_2XL = 50;
}
